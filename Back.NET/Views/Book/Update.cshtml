@model Book
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

    <div class="container mt-5">
        <a asp-controller="Book" asp-action="Index" class="btn btn-secondary mb-3">Return to List</a>
        <h2>Update New Book</h2>
        <form asp-controller="Book" asp-action="Update" method="POST">
            <!-- For bakcend !-->
            <input type="hidden" name="id" value="@Model.Id" />
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" class="form-control" id="title" name="title" placeholder="Enter title" value="@Model.Title"> <!-- For bakcend !-->
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea class="form-control" id="description" rows="4" name="description" placeholder="Enter description">@Model.Description</textarea> <!-- For bakcend !-->
            </div>
            <div class="form-group">
                <label for="author">Author:</label>
                <input type="text" class="form-control" id="author" name="author" placeholder="Enter author" value="@Model.Author"> <!-- For bakcend !-->
            </div>
            <div class="form-group">
                <label for="price">Price:</label>
                <input type="number" class="form-control" id="price" name="price" placeholder="Enter price" value="@Model.Price"> <!-- For bakcend !-->
            </div>

            <button type="submit" class="btn btn-primary">Save</button> <!-- For bakcend !-->
        </form>
    </div>

</body>
</html>
